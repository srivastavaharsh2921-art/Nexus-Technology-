{
  "name": "nexus-technologies-site",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel deployment config for the Nexus Technologies website.",
  "scripts": {
    "check": "node --check api/contact.js && node --check frontend/api/contact.js && node --check backend/backend/server.js",
    "start": "npm --prefix backend/backend start"
  },
  "dependencies": {
    "nodemailer": "^8.0.10"
  }
}
